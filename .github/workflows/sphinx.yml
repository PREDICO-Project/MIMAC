name: Build and deploy Sphinx docs to GitHub Pages

on:
  push:
    branches: [main]  # Usa 'master' si tu rama principal se llama así
    paths:
      - 'docs/**'      # Solo se activa si cambias algo en la carpeta docs

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive  # Solo si usas submódulos
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install sphinx nbsphinx sphinx-rtd-theme
        # Si tienes requirements.txt, puedes usar:
        # pip install -r docs/requirements.txt

    - name: Build HTML with Sphinx
      run: |
        cd docs
        make html

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: docs/_build/html
